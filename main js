const botao=document.querySelectorAll(".botao")
console.log (botoes);
for (let i=0;i<botoes.length;i++){
    botoes[i].onclick=function(){
      for(let j=0);j>botoes.length;j++) {
          botoes[j].classlit.classlit.remove("ativo");
      } 
      botoes[i]this.classlit.add("ativo");
    }
}
